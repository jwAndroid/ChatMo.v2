import { View, Text } from 'react-native';
import React, { memo } from 'react';

function ButtonBar() {
  return (
    <View>
      <Text>ButtonBar</Text>
    </View>
  );
}

export default memo(ButtonBar);
